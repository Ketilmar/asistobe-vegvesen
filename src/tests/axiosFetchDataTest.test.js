//import { fetchApi } from "../utils/axiosFetchDataUtils.js";
//import {jest} from '@jest/globals'
//import mockAxios from "axios"


// jest.mock("axios", () => ({
//     __esModule: true,
//     default: {
//         get: jest.fn().mockResolvedValue({ data: { trafficData: { trafficRegistrationPoint: { name: "Mocked Value"}} }})
//     },
// }));

// jest.mock("axios", () => ({
//     __esModule: true,
// }));
// mockAxios.get.mockResolvedValue({ data: { trafficData: { trafficRegistrationPoint: { name: "Mocked Value"}} }})

test("should return data", async () => {
    // const result = await fetchApi();
    // console.log(result);
    // expect(result).toBe("Mocked Value");
});